## Idea central

La trayectoria es el rastro geométrico del movimiento, pero también una memoria visual del método numérico y del campo aplicado. Cambiar el método o el entorno cambia la curva observada.

La misma curva puede recorrerse rápido o lento. La forma depende de la dirección de la velocidad; el tiempo de viaje depende también de su módulo.

## Ejercicio resuelto

**Problema.** Si

$$
\mathbf{r}(t)=(2t,t^2)
$$

entonces

$$
\mathbf{v}(t)=(2,2t),
\qquad
\mathbf{a}(t)=(0,2)
$$

Para $$t=1$$,

$$
\frac{dy}{dx}=\frac{2t}{2}=1
$$

La tangente forma un ángulo de $$45^\circ$$.

## Qué observar en la simulación

La traza del bote es una curva paramétrica discreta. Cada punto corresponde a un instante de integración y sirve para comparar métodos y escenarios.
